# Copy to keystore.properties (same directory) and fill with your real values.
# Do NOT commit keystore.properties or your keystore file.

storeFile=/absolute/path/to/your-release-keystore.jks
storePassword=CHANGE_ME
keyAlias=release
keyPassword=CHANGE_ME
